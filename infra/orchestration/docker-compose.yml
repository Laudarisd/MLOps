version: '3.8'
services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
  celery:
    build: .
    command: celery -A sample_celery_app worker --loglevel=info
    volumes:
      - .:/app
    depends_on:
      - redis
